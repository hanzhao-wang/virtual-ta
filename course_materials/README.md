Place local course materials here if you prefer the legacy layout.

The default student upload folder is now `resources/`.

Suggested layout:
- `lectures/`
- `tutorials/`
- `assignments/`
- `quizzes/`
- `reference/`

The scanner walks these folders recursively, so nested week/topic folders are fine.
